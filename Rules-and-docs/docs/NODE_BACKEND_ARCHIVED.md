# Node Express backend — archived (PHP is primary)

The old `backend/` Express app was removed after the PHP cutover.

| Use | Path |
|-----|------|
| Active API | `backend/` |
| Photos | `backend/uploads/` |
| Local run | `backend/start.ps1` → `:5080` |
| Hostinger | see `HOSTINGER_DEPLOY.md` |

If you need the old Node code, restore it from **git history** before the cleanup commit.
